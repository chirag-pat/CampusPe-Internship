# requirements.txt — AI Microservice Dependencies
# Tool-04 | Risk Appetite Framework
# Sprint: 14 April – 9 May 2026
#
# Install:
#   pip install -r requirements.txt
#
# Exact versions pinned for reproducible Docker builds.
# Update versions only after testing — do not use unpinned ranges in production.

# ── Web framework ──────────────────────────────────────────────────────────
flask==3.1.0
flask-cors==4.0.1

# ── Day 3: Input sanitisation (AI Developer 3) ────────────────────────────
bleach==6.1.0                  # HTML stripping — strip_html() in sanitisation.py

# ── Day 4: Rate limiting (AI Developer 3) ─────────────────────────────────
flask-limiter==3.8.0           # Rate limiting middleware
limits==3.13.0                 # Storage backends used by flask-limiter
redis==5.0.4                   # Redis client for flask-limiter storage backend

# ── AI / Groq (AI Developer 1 & 2) ───────────────────────────────────────
groq==0.9.0                    # Official Groq Python client
# Alternative: use httpx directly — see services/groq_client.py

# ── Vector database / RAG (AI Developer 1) ────────────────────────────────
chromadb==0.5.3
sentence-transformers==3.0.1
torch==2.3.1                   # Required by sentence-transformers

# ── HTTP client ───────────────────────────────────────────────────────────
httpx==0.27.0                  # Used by groq SDK internally

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # Load .env file for local development
pydantic==2.7.1                # Request/response validation models

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-flask==1.3.0
pytest-cov==5.0.0
